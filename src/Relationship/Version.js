import Version from './Version';
import React from 'react';
import { Text } from 'react-native';
import Style from './Sytle';

import Origin from './Origin';

export default (props) => {
  return <>{props.clidren}</>;
};
